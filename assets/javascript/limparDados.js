/* Função que limpa a tela do navegador, resetando tudo que foi digitado em input, optei por usar o reload() afim de simplificar o código, ao inves de inserir valores em vazio, aumentando assim a verbosidade do código */

function limparDados() {
    location.reload();
}