/* Função que adiciona uma classe CSS dentro do html para que seja possivel aplicar o efeito de fadeIn no texto */

function fadeInAnimation(id) {
    document.getElementById(id).classList.add('fadeInAnimation');
}